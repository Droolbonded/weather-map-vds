kind = "web"
previewPath = "/"
title = "ESP32 Hava Durumu Haritasi"
version = "1.0.0"
id = "artifacts/esp32-map"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18100

[services.development]
run = "pnpm --filter @workspace/esp32-map run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/esp32-map", "run", "build" ]
publicDir = "artifacts/esp32-map/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18100"
BASE_PATH = "/"
